angular.module("testModule", ["mediaModal"])
.controller("testController", function($scope, gallery){
        $scope.openGallery = function(){
            gallery.open(true)
                .then(function(images){
                    $scope.images = images;
                },
                function(){

                }
            )
        }
    }
);